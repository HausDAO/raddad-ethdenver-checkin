export type CheckIn = {
  id: string;
  timeStamp: string;
  claimVersion: number;
  description: string;
  future: string;
  memberAddress: string;
  morale: string;
  secondsWorked: string;
  obstacles: string;
  tokenAmountClaimed: string;
};
